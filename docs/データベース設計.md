# データベース設計

## テーブル一覧

### users

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | Supabase AuthのユーザーID |
| name | text | 表示名（プロフィール画面で編集可） |
| bio | text | 自己紹介（任意・プロフィール画面で編集可） |
| created_at | timestamp | 作成日時 |
| updated_at | timestamp | 更新日時（UPDATE時にトリガで自動更新） |

---

### groups

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | グループID |
| name | text | グループ名 |
| invite_code | text (UNIQUE) | 招待コード（自動生成） |
| owner_id | uuid (FK: users.id) | 作成者 |
| created_at | timestamp | 作成日時 |

---

### group_members

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | |
| group_id | uuid (FK: groups.id) | |
| user_id | uuid (FK: users.id) | |
| joined_at | timestamp | 参加日時 |

- (group_id, user_id) にUNIQUE制約

---

### posts

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | 投稿ID |
| user_id | uuid (FK: users.id) | 投稿者 |
| video_url | text | Supabase StorageのURL |
| needs_flip | boolean | 上下逆で記録された動画の補正フラグ（Android前面カメラ等） |
| platform | text | 投稿元（`web` / `mobile`）。再生時の回転補正に使う |
| created_at | timestamp | 投稿日時 |

---

### post_shares

投稿と共有先グループを分離するテーブル。1投稿を複数グループに共有できる構造。

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | |
| post_id | uuid (FK: posts.id) | |
| group_id | uuid (FK: groups.id) | 共有先グループ |
| user_id | uuid (FK: users.id) | 投稿者 |
| shared_date | date | 共有した日付（日本時間） |
| shared_hour | int | 共有時の時間帯（0〜23） |
| created_at | timestamp | 共有日時 |

- 同一グループ・同一ユーザー・同日・同時間帯は1回まで: `UNIQUE (group_id, user_id, shared_date, shared_hour)`
- ※ `date(created_at)` のような式インデックスはPostgresのIMMUTABLE制約で使えないため、日付は `shared_date` カラムとして明示的に保存する

---

### analytics_events

| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid (PK) | |
| user_id | uuid (FK: auth.users.id) | 操作したユーザー |
| event_name | text | イベント名 |
| properties | jsonb | イベント付属データ |
| created_at | timestamp | 発生日時 |

---

## ER図（簡易）

```
users ──< group_members >── groups
  │                             │
  ├──< posts >── post_shares >──┘
  │
  └──< analytics_events
```

---
